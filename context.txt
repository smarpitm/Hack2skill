=== CHAT CONTEXT ===
User: Smarpit
Project: AI Candidate Ranking System for India Runs Hackathon
Track: Track 1 - The Data & AI Challenge (Redrob & Hack2skill)
Prize: 10 Lakhs
Timeline: 42-day virtual hackathon

ARCHITECTURE DECIDED:
- 2-Stage Local Retrieval and Ranking Pipeline
  Stage 1: Sentence-BERT + FAISS (dense retrieval, Top-200)
  Stage 2: XGBoost ranker (15 features, synthetic labels, Top-100) + Rule-based Reasoning

CONSTRAINTS:
- ZERO paid APIs in core pipeline
- Zero network calls / GROQ removed to guarantee local CPU execution
- Everything runs locally
- No website needed — submission is code + documentation + CSV

KEY DECISIONS MADE:
- Using agentic AI (Cursor/Claude) with chunked prompts
- context.txt as bridge file for every prompt
- Project structure: src/ folder with __init__.py, config.py, preprocessing.py, data_loader.py, job_description.py, reasoning_generator.py etc.

CURRENT STATUS:
- Core Pipeline Refactoring COMPLETE:
  └─ Config optimized for local execution (removed GROQ settings)
  └─ Added src/data_loader.py and src/job_description.py to modularize loading
  └─ Modified src/features.py to calculate exactly 15 features including honeypot checks/penalties
  └─ Modified src/pipeline.py to process single-job locally, enforce deterministic candidate_id ascending tie-breaker, and generate reasoning using reasoning_generator.py
- Removed LLM reranker completely (deleted src/llm_reranker.py, removed groq from requirements.txt)
- Added tests/test_e2e.py to perform end-to-end simulation (collects 220 unit tests)
- All 220 tests passing successfully ✅
- Validated on sample candidates pool, conforming strictly to validate_submission.py checks ✅

TECH STACK:
- Python 3.9+, sentence-transformers, faiss-cpu, xgboost, pandas, python-dotenv
- Virtual environment: venv/ in project root
- Environment: Windows / PowerShell

INDIAN CONTEXT RULES:
- B.Tech == B.E., MCA == M.Tech CS, NIT/IIT = Tier-1 signal
- Tier-2/3 + high platform activity = strong positive signal
- Location normalization for Indian cities

PROJECT FILES (src/):
- config.py          → All constants, paths
- preprocessing.py   → Text cleaning, normalization
- data_loader.py     → Parses candidates JSON/JSONL/GZ/CSV
- job_description.py → Parses job description metadata
- embeddings.py      → Sentence-BERT + FAISS index
- features.py        → 15-feature extraction for XGBoost
- synthetic_labels.py→ Generates training labels
- ranker.py          → XGBoost train/predict
- reasoning_generator.py → Generates deterministic candidate reasoning

USER PREFERENCES:
- Wants free models only (no paid APIs)
- Cache must be persistent across script runs
- On ANY error, return None — NEVER crash
- Windows/PowerShell environment

=== END CONTEXT ===
